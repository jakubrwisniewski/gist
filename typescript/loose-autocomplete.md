# Loose autocomplete in TypeScript

```ts
type Names = "John" | "Jane" | "David" | (string & {});
```

Above will let developer to choose name from autocomplete in editor and also pass any string.
